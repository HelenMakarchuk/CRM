<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CRM.Views.PaymentPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <ContentPage.ToolbarItems>
        <ToolbarItem
            x:Name="EditToolbarItem"
            Clicked="Edit_Clicked"
            Order="Primary"
            Priority="2" />
        <ToolbarItem
            x:Name="DeleteToolbarItem"
            Clicked="Delete_Clicked"
            Order="Primary"
            Priority="1" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout>
            <Entry
                x:Name="SumEntry"
                Margin="10,20,10,0"
                FontSize="16"
                Placeholder="Sum"
                PlaceholderColor="#bababa" />
            <Entry
                x:Name="StatusEntry"
                Margin="10,10,10,0"
                FontSize="16"
                Placeholder="Status"
                PlaceholderColor="#bababa" />
            <Entry
                x:Name="MethodEntry"
                Margin="10,10,10,0"
                FontSize="16"
                Placeholder="Method"
                PlaceholderColor="#bababa" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>